version: "3.8"

services:
  agent-ai:
    build: .
    container_name: agent-ai
    ports:
      - "8000:8000"
    volumes:
      - ./:/app:delegated
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
